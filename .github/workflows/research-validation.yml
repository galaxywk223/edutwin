name: Research validation

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Verify complete research delivery
        shell: pwsh
        run: ./scripts/verify-delivery.ps1 -Target Local
